import java.util.Hashtable;

/**
 * An edge of a graph.
 *
 *code taken from the net/datastructures directory code from the book 
 */
public interface Edge<E>{
  /** Returns the element associated with the edge. */
  E getElement();
}